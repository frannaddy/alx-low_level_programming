#include <studio.h>
/**
 * main - program prints out all single numbers of base
 * Return: Always 0 (success)
 */
int mai(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}

